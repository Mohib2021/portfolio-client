import React from "react";
import Particles from "react-tsparticles";
import ParticleConfig from "./Particle.config";
function Particle() {
	return <ParticleConfig params={Particles} />;
}

export default Particle;
